package;

/** This entity is expected to be spawnable in-game, like from Sandbox's spawnmenu. */
@:native("SpawnableAttribute")
final extern class SpawnableAttribute extends TagAttribute {
    function new():Void;
}
