package sandbox;

/** When added to a string property, will allow selection of anything that a Texture can be */
@:native("Sandbox.TextureImagePathAttribute")
extern class TextureImagePathAttribute extends system.Attribute {
    function new():Void;
}
