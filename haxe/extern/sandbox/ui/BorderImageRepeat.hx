package sandbox.ui;

/** Possible values for border-image-repeat (border-image) CSS property. */
@:native("Sandbox.UI.BorderImageRepeat")
extern enum abstract BorderImageRepeat(Int) {
    var Stretch;
    var Round;
}
