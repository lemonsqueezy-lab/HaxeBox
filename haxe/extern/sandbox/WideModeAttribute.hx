package sandbox;

/** Expand the value editor to fill the next line in the inspector, leaving the title above it */
@:native("Sandbox.WideModeAttribute")
final extern class WideModeAttribute {
    function new():Void;
    var HasLabel(default,default):Bool;
    var TypeId(default,never):cs.system.Object;
}
