package sandbox.moviemaker;

/** Unused, will describe running actions in the scene. */
@:native("Sandbox.MovieMaker.IActionTrack")
extern class IActionTrack {
    var Parent(default,never):sandbox.moviemaker.ITrack;
}
