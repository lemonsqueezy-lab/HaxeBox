package sandbox.moviemaker.properties;

/** Dummy type for to return if it matches a track, but doesn't know what target type it maps to. */
@:native("Sandbox.MovieMaker.Properties.Unknown")
extern class Unknown {
}
